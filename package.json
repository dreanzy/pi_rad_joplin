{
  "name": "rad-joplin",
  "version": "0.1.0",
  "description": "Joplin extension tools for Pi Agent.",
  "license": "MIT",
  "type": "module",
  "files": [
    "extensions"
  ],
  "exports": {
    "./extensions": "./extensions/index.ts"
  },
  "pi": {
    "extensions": [
      "./extensions"
    ]
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "ci": "npm run typecheck && npm test"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*",
    "typebox": "*"
  },
  "devDependencies": {
    "@earendil-works/pi-coding-agent": "^0.79.6",
    "@types/node": "^26.0.0",
    "typebox": "^1.3.1",
    "typescript": "^6.0.3",
    "vitest": "^4.1.7"
  }
}
